{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "12.18.0"
  },
  "scripts": {
    "start": "node cd client && npm install && npm run build && cd .. && node backend/app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "express-fileupload": "^1.2.0",
    "form-data": "^3.0.0",
    "helmet": "^4.2.0",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "jimp": "^0.16.1",
    "nodemon": "^2.0.5"
  }
}
